<p style="color: green"><%= notice %></p>

<% content_for :title, "Threads" %>

<h1>Threads</h1>

<div id="threads">
  <% @threads.each do |thread| %>
    <%= render thread %>
    <p>
      <%= link_to "Show this thread", thread %>
    </p>
  <% end %>
</div>

<%= link_to "New thread", new_thread_path %>
